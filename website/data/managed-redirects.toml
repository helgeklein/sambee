# Managed redirect source paths are public product contracts. Do not rename,
# remove, or reuse a source path after it is released. Update only its target.

[[redirects]]
id = "help-oidc-setup"
source = "/mr/help-oidc-setup"
target = "https://sambee.net/docs/admin-guide/authentication/openid-connect-authentication-setup/"
